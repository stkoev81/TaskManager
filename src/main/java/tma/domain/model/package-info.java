/**
 * Classes (entities and possibly value objects) that represent the essential state and behavior of the applicaiton's
 * domain. These may be saved and retrieved from persistent storage by other layers <br/>
 * 
 * Domain objects are concerned only with domain logic (also called business logic), and not with with technology (such
 * as persistence, web).
 * 
 * @author skoev
 * 
 */
package tma.domain.model;

